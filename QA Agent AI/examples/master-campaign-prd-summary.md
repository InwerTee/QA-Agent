# Master Campaign PRD Context

The Master Campaign module includes a Master Campaign List Page for Admin users.

Relevant page behavior for this test:

- The page has a Search Bar.
- The list table displays Master Campaign records.
- Searching by a keyword should update the table results.
- If no Master Campaign name matches the keyword, the page should show an empty state instead of unrelated records.
- The entered keyword should remain visible in the Search Bar after the search runs.

This context is intentionally minimal. Replace it with the real PRD text when testing with OpenAI planning.
